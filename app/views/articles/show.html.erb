<div class="container">
  <div class="row">
    <div class="col-4">
      <h1><%= @article.title %></h1>
      <small>Posted: <%= @article.created_at.strftime("%m/%d/%Y") %></small>
      <p><%= @article.body %></p>

      <%= link_to('Edit', edit_article_path) %>

      <%= link_to('Delete', article_path, method: :delete) %>
    </div>
  </div>
</div>